interface:
  display_name: "LLM Wiki"
  short_description: "Route LLM Wiki bootstrap, ingest, expression assets, query, and lint"
  default_prompt: "Use $llm-wiki to process my Obsidian source or conversation into the right wiki workflow, including AI_Media expression assets when useful or requested."

policy:
  allow_implicit_invocation: true
